Saved Posts / Collections

Backend tasks:
- Create `Saved` model linking `userId`, `postId`, and `collectionName`
- APIs to save/unsave posts and manage collections

Frontend tasks:
- Save button on posts, UI to add to collection
- Saved posts view in user profile
