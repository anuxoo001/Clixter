Post Analytics & Engagement Insights

Backend tasks:
- Collect metrics (views, impressions, clicks) in `PostMetrics` model
- APIs to return aggregated stats per post

Frontend tasks:
- Dashboard for users to view analytics per post
