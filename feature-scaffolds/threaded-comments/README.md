Threaded (Nested) Comments

Backend tasks:
- Update `Comment` model to support `parentId` for nesting
- APIs for creating/retrieving threaded comments (with pagination)

Frontend tasks:
- UI for replying to specific comments
- Collapse/expand nested threads
