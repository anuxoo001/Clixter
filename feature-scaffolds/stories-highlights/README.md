Stories with Highlights & Expiry

Backend tasks:
- Create `Story` model with `media`, `expiresAt`, `userId`
- Worker to purge expired stories and update status
- APIs to create, fetch active stories, and create highlights

Frontend tasks:
- Story creation UI (camera/upload), story viewer carousel
- Highlights on profile (persistent collections)
