{
  "name": "clixter-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install": "npm --prefix client install",
    "build": "npm --prefix client run build",
    "dev": "npm --prefix client run dev"
  }
}

